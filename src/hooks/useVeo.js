// TODO: integrate Google Veo API for destination video generation
export function useVeo() {
  const generateDestinationVideo = (placeName, description) => null
  return { generateDestinationVideo }
}
